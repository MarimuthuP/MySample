<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="WithActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextLabel_UpdateProfile" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">14sp</item>
        <item name="android:textColorHint">@color/dark_blue</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorAccent">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/black</item>
        <item name="colorPrimary">@color/black</item>
    </style>

    <style name="AppThemePopup" parent="android:Theme.Light" />
    <style name="PauseDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/PauseDialogAnimation</item>
    </style>

    <style name="PauseDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/fadein</item>
        <item name="android:windowExitAnimation">@anim/fadeout</item>
    </style>

    <style name="spinner_style" >
        <item name="android:background">@drawable/spinner_bg</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
    </style>

    <style name="GreenTheme" parent="Theme.AppCompat">
        <item name="colorAccent">@color/greenColor</item>
        <item name="android:textColorSecondary">@color/redColor</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Gets rid of the search icon -->
        <item name="searchIcon">@mipmap/ic_search</item>
        <!-- Gets rid of the "underline" in the text -->
        <item name="queryBackground">@null</item>
        <!-- Gets rid of the search icon when the SearchView is expanded -->
        <item name="searchHintIcon">@null</item>
        <!-- The hint text that appears when the user has not typed anything -->
        <item name="queryHint">@string/search_hint</item>
    </style>

    <!-- Green Dialog Theme -->
    <style name="AppTheme.Dialog.Green" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/redColor</item>
        <item name="colorPrimaryDark">@color/redColor</item>
        <item name="colorAccent">@color/redColor</item>
    </style>

    <!-- Green Alert Dialog Theme -->
    <style name="AppThemeDialogAlertGreen" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/greenColor</item>
        <item name="android:textColor">@color/redColor</item>
    </style>

    <!-- Yellow Theme -->
    <style name="YellowCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/yellowColor</item>
    </style>

    <!-- Green Theme -->
    <style name="GreenCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/yellowColor</item>
        <item name="colorControlActivated">@color/greenColor</item>
    </style>

    <!-- Pink Theme -->
    <style name="PinkCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/greenColor</item>
        <item name="colorControlActivated">@color/pinkColor</item>
    </style>

    <!-- Blue Theme -->
    <style name="BlueCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/pinkColor</item>
        <item name="colorControlActivated">@color/blueColor</item>
    </style>

</resources>
